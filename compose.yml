version: "3"
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    tty: true
    volumes:
      - .:/go/src/app
    ports:
      - "18888:18888"
    working_dir: /go/src/app
